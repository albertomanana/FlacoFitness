# Decisions Log

## Como registrar una decision

Usar una entrada por decision tecnica relevante. Mantener el registro en orden cronologico y reflejar tanto el motivo como el impacto esperado.

## Plantilla

| Fecha | Decision | Motivo | Impacto |
| --- | --- | --- | --- |
| AAAA-MM-DD | Describir la decision tecnica | Explicar por que se toma | Indicar consecuencias, beneficios o limitaciones |

## Registro inicial

| Fecha | Decision | Motivo | Impacto |
| --- | --- | --- | --- |
| 2026-03-23 | Adoptar una arquitectura monolitica MVC con Spring Boot y Thymeleaf | Se ajusta al alcance academico, facilita despliegue y simplifica la defensa del proyecto | Permite avanzar rapido con una base coherente y mantenible |
| 2026-03-24 | Adoptar flujo de ramas con `main` estable y `develop` como integracion | Mejora trazabilidad y reduce riesgo de mezclar trabajo inestable con entregas | Facilita colaboracion, revision y automatizacion del repositorio |
| 2026-04-07 | Ejecutar una alineacion automatica del esquema de `rutinas` al arrancar | La base MySQL conservaba `rutinas.usuario_id` aunque el dominio ya usa una relacion `ManyToMany` con `usuario_rutina` | Se migran relaciones legacy sin perder datos y se evita el error 500 al crear o editar rutinas |
| 2026-04-07 | Consolidar el dashboard con un endpoint agregado `/stats/dashboard` | El dashboard necesitaba mas metricas y series sin multiplicar llamadas ni complejidad en el frontend | Se simplifica la explicacion academica, se centraliza la lectura de KPIs y se mantiene Chart.js con JS ligero |
| 2026-04-07 | Resolver la URL publica de fotos de usuario mediante un componente dedicado | Los `fotoPath` existentes podian llegar en formatos distintos y la UI no tenia un fallback uniforme | Se asegura la visualizacion correcta en lista, detalle y formulario, con avatar por defecto sin romper usuarios existentes |
| 2026-04-07 | Integrar DataTables solo en listados clave y tabla reciente del dashboard | Se buscaba mejorar busqueda, ordenacion y paginacion sin convertir la aplicacion en SPA ni duplicar logica | Se gana usabilidad real con una libreria conocida y justificable, manteniendo Thymeleaf y controladores MVC intactos |
| 2026-04-08 | Reestructurar el fragmento `footer` para inyectar scripts globales desde Thymeleaf | El fragmento anterior solo devolvia el nodo `<footer>`, dejando fuera Chart.js, DataTables y el JS propio | Los graficos, las tablas interactivas y las mejoras de UX se aplican de forma consistente en todas las vistas |
| 2026-04-08 | Anadir `jQuery` como dependencia ligera de frontend para DataTables con Bootstrap 5 | La combinacion usada de DataTables y sus adaptadores Bootstrap/Responsive fallaba en navegador por `jQuery is not defined` | Se recupera la busqueda, paginacion y ordenacion sin rehacer los listados ni cambiar el stack MVC |
| 2026-04-08 | Simplificar el copy del panel y reforzar el contraste del sidebar | La interfaz seguia mostrando demasiado texto explicativo y el menu lateral perdia legibilidad frente al fondo | El sistema visual queda mas cercano a un SaaS administrativo real, con mejor jerarquia visual y explicacion academica mas clara |
| 2026-04-08 | Mover la subida de foto de usuario a la pantalla de edicion y dejar el detalle solo como consulta | La operacion de cambiar imagen pertenece al flujo de actualizacion del registro y el panel lateral del detalle estaba mezclando lectura con edicion | La UX queda mas coherente, la defensa academica es mas facil de explicar y la tarjeta de foto se puede mantener sticky sin interferencias |
| 2026-04-08 | Centralizar el dashboard en `/stats/dashboard` con un parametro `rangoDias` | El panel principal ya necesitaba mas indicadores y series, pero mantener varios `fetch` hacia endpoints independientes complicaba la sincronizacion y la explicacion academica | Se simplifica el frontend, se controla mejor el filtro temporal y se mantiene una historia de datos mas clara para la demo |
| 2026-04-08 | Implementar un `ControllerAdvice` especifico para vistas MVC | Las excepciones de negocio y de recurso inexistente estaban cayendo en paginas genericas o poco coherentes con la interfaz principal | La aplicacion gana continuidad visual, mensajes mas claros y una defensa tecnica mas ordenada alrededor del flujo MVC |
| 2026-04-08 | Mostrar ayudas dinamicas en los formularios de pago y usuario con JavaScript ligero | Se buscaba mas claridad operativa sin introducir SPA, duplicar logica de negocio ni sobrecargar el stack | El usuario entiende mejor que el importe del pago se deriva del plan y puede validar la foto antes de subirla, manteniendo Thymeleaf como capa principal |
| 2026-04-08 | Corregir Chart.js con deep-merge de opciones y exclusion de escalas en charts radiales | El spread superficial anterior sobrescribia la configuracion base de plugins y tooltips, y los canvases con opacity:0 reportaban dimensiones 0x0 a Chart.js | Los graficos vuelven a renderizar en el dashboard, los doughnut charts no reciben escalas cartesianas invalidas y las chart-cards se muestran inmediatamente sin esperar al observer |
| 2026-04-08 | Refactorizar todo el CSS a filosofia mobile-first con design tokens expandidos | La hoja de estilos anterior usaba reglas desktop-first con overrides descendentes, generando inconsistencias en movil y duplicacion de propiedades | Se reduce el tamano efectivo del CSS, se mejora la experiencia en movil y se establece un sistema de tokens reutilizable para futuras vistas |
| 2026-04-08 | Adoptar SortableJS para reordenacion de KPI cards en el dashboard | Se buscaba una interaccion drag and drop util y estable para la defensa academica sin reimplementar touch handling nativo | Se gana una demo interactiva de UX avanzada con persistencia en localStorage y fallback automatico si la libreria no carga |
| 2026-04-09 | Deshabilitar `spring.sql.init.mode` por defecto en MySQL | Evitar que `data.sql` contamine la base real y genere discrepancias entre lo que se ve en UI y lo que existe en MySQL | Una sola verdad de datos por ejecucion y defensa academica mas explicable |
| 2026-04-09 | Servir Chart.js desde recurso local versionado y no depender del CDN para el dashboard | Los graficos no deben depender de conectividad externa y el asset debe ser estable para la defensa | Dashboard mas fiable y determinista sin cambiar stack ni convertir la app en SPA |
| 2026-04-09 | UX de usuarios: fila clicable y detalle sin cambio de foto | Reducir ruido en listados y mantener el detalle como vista de consulta; los cambios se hacen desde editar | Navegacion mas rapida, interfaz mas limpia y coherente; flujo de edicion mas defendible |
| 2026-04-09 | Serializar el estado inicial del dashboard a JSON explicito desde el controlador | La serializacion implicita del template era mas fragil y dificultaba el debug del estado inicial | El dashboard arranca con datos renderizados por servidor y mantiene un contrato JSON claro y testeable |
| 2026-04-09 | Tratar el fallo del dashboard como regresion de render de la home y no como bug aislado de Chart.js | La causa raiz real era una expresion Thymeleaf que rompia la vista al truncar referencias cortas de pago | Se recupera la home completa, vuelven a cargarse los charts y el sistema queda mas defendible porque el error queda explicado de extremo a extremo |
| 2026-04-10 | Construir un agregado especifico para la ficha de usuario y no persistir score ni segmento en base de datos | El detalle necesitaba integrar pagos, asistencias y rutinas sin duplicar estado ni contaminar la entidad `Usuario` con logica de presentacion | La ficha se vuelve un centro de control defendible, el controlador sigue limpio y el calculo queda encapsulado en un servicio reutilizable |
| 2026-04-10 | Reutilizar la vista de rutinas con una ruta filtrada por usuario | El acceso rapido desde usuario necesitaba una salida real hacia rutinas sin crear una pantalla paralela ni duplicar HTML | Se mantiene una sola biblioteca de rutinas, pero ahora puede abrirse en contexto del usuario para demo y gestion diaria |
| 2026-04-13 | Definir el ciclo financiero por `usuario + fecha_vencimiento` en pagos | La duplicidad detectada en pagos provenia de altas/ediciones sin regla de unicidad funcional del ciclo mensual | Se evita crear cuotas repetidas y se mantiene trazabilidad limpia sin romper seeds ni rutas existentes |
| 2026-04-13 | Sustituir boton "ver" por fila clicable en pagos y conservar enlace explicito al usuario | La navegacion era redundante y lenta para operacion diaria, pero se necesitaba preservar acceso directo a ficha de miembro | UX mas natural (click row), menos ruido en acciones y continuidad con el patron ya aplicado en asistencias |
| 2026-04-13 | Mantener monto derivado desde plan y reforzar formulario con vencimiento obligatorio y asistente de contexto | El formulario permitia combinaciones manuales poco coherentes para un modulo academico de control de cuotas | Menos error operativo, mayor coherencia con membresias y defensa tecnica mas clara al explicar reglas de negocio |
| 2026-04-13 | Eliminar `objetivo` del dominio de rutinas en lugar de ocultarlo solo en la interfaz | El campo era ornamental y generaba ruido en formularios, listados, detalles, dashboard y semillas | Se simplifica el modelo, se reduce mantenimiento y la UX queda mas coherente entre modulos |
| 2026-04-13 | Estandarizar rutinas con filas clicables y acciones minimas | Rutinas seguia teniendo un patron de navegacion distinto al de asistencias y pagos | Mejor cohesion visual y menor carga cognitiva para el usuario al moverse entre listados |
| 2026-04-13 | Introducir un acceso MVP por PIN con sesion e interceptor propio | No existia ninguna capa de acceso y se necesitaba un control simple, explicable y consistente con la defensa academica | Se protege la navegacion del panel con un flujo minimo, limitacion de intentos y bloqueo temporal sin meter Spring Security |
| 2026-04-13 | Convertir notificaciones del topbar en alertas accionables basadas en estados del negocio | El icono de campana era solo decorativo y no aportaba contexto operativo | El topbar pasa a mostrar alertas reales con enlaces a pagos, usuarios y asistencias |
| 2026-04-13 | Reorganizar el sidebar en bloques de visibilidad, operaciones y acceso | El menu seguia siendo una lista unica y no ayudaba a explicar el alcance del panel | La navegacion gana jerarquia y el cierre de acceso queda disponible desde la misma shell |
| 2026-04-15 | Modelar staff como `StaffPerfil` ligado a `Usuario` | Staff representa personas ya registradas en el sistema; duplicarlo como entidad independiente crearia inconsistencias de identidad y contacto | Se gana un modulo operativo real sin duplicar datos personales y se mantiene compatible con roles actuales |
| 2026-04-15 | Separar `Plan`, `MembresiaUsuario` y `Pago` | Un producto vendible necesita distinguir oferta comercial, contrato de cliente y cobro financiero | El modelo financiero queda mas claro, los pagos legacy siguen cargando y se prepara renovacion/deuda sin romper `Usuario.plan` |
| 2026-04-15 | Separar `Clase` de `SesionClase` | Una clase como Yoga/HIIT no es lo mismo que una ocurrencia con fecha, hora, cupo y staff | Permite agenda, reservas, cupos y asistencia por sesion de forma explicable y extensible |
| 2026-04-15 | Mantener `Asistencia` libre y anadir `SesionClase` opcional | El gimnasio necesita check-in general y tambien control de asistencia a clases concretas | No se rompe el historial existente y se habilita una operacion diaria mas realista |
| 2026-04-15 | Dejar `data.sql` seguro para MySQL y mover la demo rica a un seeder desactivable | Las semillas completas pueden contaminar bases reales y dificultar la defensa de una sola fuente de verdad | MySQL conserva datos reales y la demo solo se ejecuta si se activa explicitamente |
| 2026-04-16 | Recuperar desde `feature/core-saas-modules` en una rama de recuperacion y preservar el estado sucio en stash | La integracion parcial actual mezclaba entidades y seeds incoherentes; resetear sin copia podia perder pistas utiles | Se vuelve al modelo avanzado defendible sin perder el trabajo previo, que queda disponible en `backup before recovery core saas plan a` |
| 2026-04-16 | Eliminar la ruta absoluta de `javac` y subir Lombok a `1.18.44` | El build dependia de `C:\Program Files\Java\jdk-17` y fallaba con JDK moderno por incompatibilidad de Lombok | El proyecto compila de forma portable con Maven Wrapper y objetivo Java 17 |
| 2026-04-16 | Mantener PIN MVP con perfiles de sesion en lugar de introducir Spring Security | El objetivo inmediato es recuperar estabilidad y permisos explicables sin sobredimensionar la seguridad | Se consigue control por rol y bloqueo de rutas con un interceptor simple, dejando Spring Security como hardening futuro |
| 2026-04-16 | Definir `puedeImpartirClases` en `StaffPerfil` | No todos los perfiles internos deben aparecer como instructores; gerencia no debe impartir clases por accidente | Solo entrenadores o perfiles autorizados pueden ser responsables de sesiones y rutinas |
| 2026-04-16 | Crear `/cliente` como panel limitado en lugar de abrir el dashboard global al cliente | El cliente no debe ver administracion global ni finanzas generales | La experiencia queda alineada con un SaaS real: cada perfil ve solo lo que necesita |
| 2026-04-17 | Incorporar modo oscuro persistente en shell SaaS | El panel se usa en contextos largos y con distintas condiciones de luz; se buscaba continuidad UX sin romper Thymeleaf | Mejora confort visual, mantiene coherencia del sistema de diseno y evita introducir complejidad frontend innecesaria |
| 2026-04-17 | Priorizar notificaciones por criticidad operativa | La campana necesitaba pasar de informativa a accionable para direccionar decisiones diarias | Se acelera la respuesta a riesgos (pagos vencidos, inactividad, renovaciones) con enlaces directos por modulo |
| 2026-04-17 | Centralizar iconografia con Font Awesome en `head` | Se detectaron iconos inconsistentes entre modulos por dependencia no cargada de forma uniforme | Todas las vistas comparten el mismo set de iconos y se reduce riesgo de regresiones visuales |
| 2026-04-17 | Cerrar fase de estabilizacion con documentacion viva sincronizada | Habia divergencia entre estado funcional real y algunos documentos de referencia | La defensa academica queda trazable: arquitectura, roadmap, backlog y setup describen el sistema actual |
| 2026-04-20 | Adoptar MySQL como unica base de datos y eliminar fallback en memoria | El producto debe simular meses reales y conservar datos en phpMyAdmin sin reinicios destructivos ni fuentes paralelas | Toda la persistencia queda en `flacofitness`, con `ddl-auto=update` y `spring.sql.init.mode=never` |
| 2026-04-20 | Introducir un reloj operativo persistido en `app_clock_settings` | Simular meses anteriores/siguientes cambiando la fecha del negocio no debe depender de la fecha del servidor ni borrar datos | Dashboard, pagos, asistencias, sesiones, membresias, gastos, maquinaria y trials pueden operar contra una fecha controlada |
| 2026-04-20 | Desacoplar el guardado del reloj de las automatizaciones secundarias | El cambio de fecha debe ser una operacion fiable; pagos o gastos recurrentes no deben impedir que se persista la fecha simulada | El reloj queda estable para demos y las automatizaciones informan si quedan pendientes, sin bloquear el control temporal |
| 2026-04-20 | Exponer el bloque financiero como submodulos de negocio (`gastos`, `recurrentes`, `nominas`) y no como un CRUD plano | El usuario necesito una lectura profesional del area financiera con trazabilidad, automatizacion y salida documental | La app gana pantallas y rutas especificas, mantiene un flujo interno serio y deja la defensa academica mas clara |
| 2026-04-20 | Generar reportes PDF financieros desde Thymeleaf y OpenHTMLtoPDF | Se necesitaba una exportacion profesional sin introducir una pila pesada ni romper el stack MVC | Se pueden archivar gastos, recurrentes y nominas con una salida PDF coherente y mantenible |
| 2026-04-21 | Centralizar pagos, gastos recurrentes y nominas en `FinancialAutomationService` | La logica estaba repartida entre reloj, schedulers y controladores manuales, con riesgo de dobles disparos | Hay una unica fachada idempotente para vencimientos y generacion automatica financiera |
| 2026-04-21 | Usar `OperationalClockService` como fecha de negocio para vencimientos y migracion financiera | `CURDATE()` y defaults de entidades podian mezclar fecha real del servidor con fecha simulada | Pagos, gastos y reparacion de datos legacy quedan alineados con la simulacion temporal |
| 2026-04-21 | Mantener tiempo real solo para seguridad de sesion y metadatos tecnicos | El bloqueo de PIN debe depender del tiempo real para no poder evitarse moviendo el reloj operativo | Se separa claramente tiempo de negocio y tiempo tecnico, facilitando defensa y mantenimiento |
| 2026-04-22 | Retirar simulacion temporal persistida y operar solo con fecha real del sistema | La simulacion introducia complejidad operativa, deuda tecnica y riesgo de inconsistencias al coexistir con usos de fecha real | `OperationalClockService` se mantiene como abstraccion unica, pero sin estado persistido ni endpoints/UI de ajuste |
| 2026-04-22 | Hacer que `.ff-main` sea visible por defecto y tratar splash/transiciones como mejora no bloqueante | Un error o carrera en la shell premium dejaba modulos completos en blanco aunque el backend respondiera correctamente | La interfaz mantiene motion premium, pero ya no puede ocultar todo el sistema por un fallo de cliente |
| 2026-04-22 | Crear un paquete de handoff explicito para Claude Code dentro de `docs/agents-memory/` | El proyecto ya tiene demasiadas capas, reglas y decisiones como para depender de memoria conversacional | La continuidad futura queda documentada con reglas, comandos, modulos, arquitectura, riesgos y flujo recomendado |
| 2026-04-22 | Corregir `gastos/detail.html` para acceder a `gasto.gastoRecurrente.frecuencia` en lugar de `gasto.frecuencia` | La entidad `Gasto` no tiene campo `frecuencia`; ese campo pertenece a `GastoRecurrente`; el acceso directo causaba `EL1008E PropertyAccessException` en Spring EL | La vista de detalle de cualquier gasto ahora carga correctamente; el bug era silencioso en tests porque la vista de detalle no estaba cubierta por `ViewControllerTest` |
| 2026-04-22 | Conectar `SaaSSchedulerService` con `app.pagos.scheduler.cron` y `app.pagos.scheduler.enabled` desde properties | El cron estaba hardcodeado a `0 0 0 * * *` y el property `app.pagos.scheduler.enabled` documentado en `application.properties` no tenia efecto | Ahora es posible desactivar o cambiar el horario del scheduler sin recompilar; util para demostraciones y entornos de test |
| 2026-04-22 | Materializar el archivo de `.claude` en `CLAUDE.md` + `claude-plan-status.md` | El archivo de `.claude` era una guia externa; para continuidad real habia que convertirlo en instrucciones y estado consumibles dentro del repo | Claude Code puede arrancar con reglas actuales y saber que parte del plan maestro ya se ejecuto |
| 2026-04-22 | Exponer todos los filtros financieros disponibles en `/gastos` | El backend ya soportaba filtros por estado, tipo, relaciones y proveedor, pero la UI solo mostraba fecha y categoria | El modulo financiero gana utilidad real para demo y operacion sin reescribir servicios ni queries |
| 2026-04-22 | Añadir PDF individual de gasto | El modulo ya exportaba listados y nominas, pero faltaba una ficha PDF por gasto para revision o demo comercial | El bloque financiero se vuelve mas coherente y profesional a nivel documental |
| 2026-04-22 | Normalizar los templates PDF como XHTML valido | OpenHTMLtoPDF es estricto al parsear HTML y el nuevo reporte individual de gasto rompia por un `meta charset` no autocerrado | Los PDFs financieros quedan mas robustos y el endpoint `/gastos/{id}/pdf` responde sin error 500 |
| 2026-04-22 | Reutilizar `shellNotifications` como fuente del rail premium del dashboard | El sistema ya tenia alertas accionables en topbar; duplicar backend para el dashboard habria creado incoherencia y ruido | El dashboard gana un carrusel/rail util y conectado sin abrir un segundo sistema de alertas |
| 2026-04-22 | Reducir el dashboard a KPIs y charts de negocio realmente utiles | La home acumulaba demasiadas cards y una grafica secundaria que debilitaban la jerarquia visual | El panel queda mas premium, mas defendible y mas rapido de leer en demo o uso real |
| 2026-04-22 | Presentar la nomina como expediente salarial y no como simple bloque CRUD | El modulo financiero ya tenia logica y PDF, pero la vista seguia viendose demasiado generica para una demo seria | La experiencia de nominas gana valor de producto y mejora la defensa academica del modulo financiero |
| 2026-04-23 | Priorizar redirect a detalle sobre redirect a listado en acciones operativas | Tras crear, editar o cambiar estado, volver al listado cortaba demasiado el flujo y hacia que la app se sintiera academica | La navegacion gana continuidad, contexto y menos friccion para demo y uso diario |
| 2026-04-23 | Tomar `usuarios/detail` como referencia de ficha rica para modulos secundarios | Varias entidades ya tenian datos y relaciones, pero se mostraban como detalles planos y poco defendibles | `staff`, `maquinas`, `materiales`, `membresias` y `pagos` convergen hacia fichas mas conectadas sin tocar el dominio |
| 2026-04-23 | Reducir botones redundantes en listados con fila clicable | Mantener "ver" o "editar" junto a una fila entera clicable duplicaba acciones y ruido visual | Los listados se leen mejor y la accion explicita queda reservada para cambios de estado o cobro |
| 2026-04-23 | Hacer mas contextuales los destinos de `ShellNotificationService` | Varias alertas llevaban a modulos generales y perdian valor operativo | Las notificaciones ahora aterrizan en vistas mas utiles, con menos pasos hasta la accion real |
| 2026-04-23 | Persistir memoria UX y visitas recientes en MySQL por `browser_token + access_profile` | La app necesitaba recordar contexto y onboarding sin introducir autenticacion compleja ni depender solo de localStorage | Se mejora la percepcion de producto y se mantiene trazabilidad controlada, separando UI efimera local de memoria UX persistente |
| 2026-04-23 | Modelar la trazabilidad de producto con `ActivityLog` por perfil de sesion y no por usuario autenticado | El sistema usa acceso por PIN/perfil, no identidad individual persistente | Se obtiene auditoria util y defendible sin reabrir el modelo de autenticacion |
| 2026-04-23 | Limitar la busqueda global v1 a usuarios, staff y sesiones | Son los tres ejes de consulta mas frecuentes y ya cubren operacion y agenda sin disparar complejidad | Se entrega busqueda util rapido, manteniendo backlog claro para ampliar a otros modulos solo si aporta valor real |
| 2026-04-23 | Resolver inteligencia de producto en un servicio de agregacion y no en vistas o controladores | Clasificaciones de riesgo, premium/anomalia y paneles de atencion no deben dispersarse entre templates | `ProductIntelligenceService` concentra reglas explicables y reutilizables para dashboard, alertas y futuras mejoras |
| 2026-04-24 | Overhaul visual — Operations Deck (dark-first, acento cian HUD, verde preservado para marca/CTAs) | El panel ya era funcionalmente avanzado pero la interfaz no transmitia la sofisticacion interna del sistema; se buscaba estetica de command-center tactil sin caer en neones infantiles | La UI gana identidad visual premium coherente en 6 bloques incrementales: tokens glass/luminoso, shell reskinned, HUD KPI cards, DataTables y forms redibujados, Chart.js paleta tematica, stepper de nominas y empty states HUD; light mode se mantiene funcional sin redisenar |
| 2026-04-24 | Mantener `.ff-main` visible por defecto como regla inviolable al redisenar shell | Un error previo de la shell premium dejaba modulos enteros en blanco; la regla es fail-safe intocable para cualquier bloque de UI | La interfaz siempre muestra contenido aunque un script o animacion falle; ningun bloque del Operations Deck toca este invariante |
| 2026-04-23 | Reemplazar `contarUsuariosAlDia/ConDeuda/ConPagosVencidos` con queries JPQL de agregacion en `PagoRepository` | Los metodos anteriores cargaban todos los usuarios y ejecutaban 2 queries por usuario (N+1); con N usuarios activos el coste era inaceptable en produccion | Los tres KPIs del modulo de pagos ahora cuestan una query cada uno sin traer entidades a memoria |
| 2026-04-23 | Auto-crear `MembresiaUsuario` al convertir un trial a usuario si el nuevo usuario tiene plan asignado | Un trial convertido quedaba fuera del ciclo de facturacion automatica si no se creaba la membresia a mano; el coste operativo de esa omision superaba cualquier riesgo de duplicado | La conversion se cierra en una sola accion y el usuario entra automaticamente en el siguiente ciclo de cobro; se guardan tres guards para no duplicar: usuario previo, con plan y sin membresia activa |
| 2026-04-23 | Usar `getChartColors()` para adaptar Chart.js al tema en tiempo de render | Los colores hardcodeados no diferenciaban contraste entre tema claro y oscuro, quedando demasiado saturados o invisibles en dark mode | Los tres charts del dashboard calculan sus colores segun `data-theme` del elemento raiz en cada render; el toggle existente de `ff:themechange` ya forzaba re-render |
| 2026-04-24 | Sustituir el acceso compartido por PIN por autenticacion por cuenta con BCrypt | El PIN MVP ya no era suficiente: no habia identidad por cuenta, cambio de password ni defensabilidad minima para un SaaS serio | El sistema pasa a autenticar por `email/username + password`, mantiene permisos con `AccessProfile` y obliga cambio de password en cuentas bootstrap |
| 2026-04-24 | Mantener autorizacion custom MVC en lugar de introducir Spring Security web completa | El proyecto necesitaba subir de nivel en seguridad sin reabrir toda la arquitectura ni romper la demo | Se obtiene autenticacion seria por cuenta y hash seguro con complejidad contenida; Spring Security queda como hardening futuro, no como requisito inmediato |
| 2026-04-24 | Separar el flujo manual de nominas en borrador, emision, pago y cancelacion | Generar siempre una nomina ya emitida mezclaba preparacion, aprobacion y gasto financiero en una sola accion | La UX de nominas gana control operativo, evita errores y permite presentar el modulo como herramienta interna profesional |
| 2026-04-26 | Optimizar la busqueda global con queries limitadas por repositorio | El endpoint era util, pero cargar listas completas de usuarios, staff y sesiones para filtrar en memoria no escala bien ni encaja con un SaaS premium | Se mantiene el contrato JSON y la UI actual, pero el coste baja y la busqueda queda protegida por tests sin introducir SPA ni dependencias nuevas |
| 2026-04-26 | Hacer dark mode la experiencia visual principal del Command Center | La estetica tactica premium pedida funciona mejor sobre fondo oscuro y el proyecto ya tenia shell dark-first avanzada | El tema oscuro se aplica por defecto con fallback light usable, sin romper persistencia de preferencia ni rutas MVC |
| 2026-04-26 | Usar Anime.js local solo como mejora progresiva de motion | Se queria un salto en animaciones con libreria, pero sin SPA ni dependencia pesada que bloquee contenido | `hud-motion.js` se inicializa solo si `window.anime` existe y respeta `prefers-reduced-motion`; si falla, CSS mantiene la UI operativa |
